[test]
name = "writer-component-metadata"
description = "Writer: Extract React component metadata (props, hooks, state)"

[plugin]
source = "plugin.lux"

[input]
source = "input.js"

[expected]
source = "expected.js"
