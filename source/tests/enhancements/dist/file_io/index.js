// Generated by ReluxScript compiler
// Do not edit manually

module.exports = function({ types: t }) {
  
  class Component {
  constructor(name, code) {
      this.name = name;
      this.code = code;
    }
  }
  
  function test_write_basic() {
    const content = "Hello, World!";
    const path = "output/test.txt";
    fs.writeFileSync(path, content);
  }
  
  function test_read_basic() {
    const path = "input/config.json";
    const content = fs.readFileSync(path, 'utf8');
    return content;
  }
  
  function test_exists() {
    const path = "output/generated.cs";
    if (fs.existsSync(path)) {
    }
  }
  
  function test_create_dir() {
    const dir = "output/components/views";
    fs.mkdirSync(dir, { recursive: true });
  }
  
  function generate_outputs(component, output_dir) {
    fs.mkdirSync(output_dir, { recursive: true });
    const cs_path = `${output_dir}/${component.name}.cs`;
    fs.writeFileSync(cs_path, component.code);
  }
  
  function write_multiple_files(components, output_dir) {
    fs.mkdirSync(output_dir, { recursive: true });
    for (const component of components) {
      const path = `${output_dir}/${component.name}.cs`;
      fs.writeFileSync(path, component.code);
    }
  }
  
  let state = {};
  
  return {
    visitor: {

    }
  };
};
