// Test: Writer methods with self parameter
writer WriterSelfTest {

fn helper(self, node: &Expr) -> String {
    // Should handle self parameter correctly in writers
    "test".to_string()
}

fn process() {
    let result = self.helper(&some_expr);
}

}
