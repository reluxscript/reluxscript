// Generated by ReluxScript compiler
// Do not edit manually
// NOTE: SWC plugins require nightly Rust

use swc_common::{Span, DUMMY_SP, SyntaxContext};
use swc_ecma_ast::*;
use swc_ecma_visit::{Visit, VisitMut, VisitMutWith, VisitWith};

mod test_module;
use test_module::{Counter, increment, is_positive};

#[derive(Clone, Debug)]
struct State {
    counter: Counter,
    total: i32,
}

pub struct TestImport {
    pub state: State,
}

impl VisitMut for TestImport {
    fn visit_mut_ident(&mut self, node: &mut Ident) {
        increment(&mut self.state.counter);
        if is_positive(self.state.counter.value) {
            self.state.total = (self.state.total + 1)
        }
    }
    
}

impl TestImport {
    pub fn new() -> Self {
        Self {
            state: State {
                counter: Default::default(),
                total: 0,
            },
        }
    }
    
    fn init() -> State {
        State { counter: Counter { value: 0 }, total: 0 }
    }
    
}

