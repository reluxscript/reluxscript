// Module imports
const fs = require('fs');
const json = {
  stringify: (obj) => JSON.stringify(obj, null, 2),
  parse: (str) => JSON.parse(str)
};
const { process_component, ProcessedComponent } = require('./process_component.js');
const { generate_csharp_file } = require('./generators/csharp_file.js');
const { generate_template_map_json } = require('./extractors/templates.js');

// Generated by ReluxScript compiler
// Do not edit manually

module.exports = function({ types: t }) {
  const builder = {
    _output: [],
    _indentLevel: 0,
    append(s) { this._output.push(s); },
    newline() { this._output.push('\n'); },
    indent() { this._indentLevel++; },
    dedent() { this._indentLevel--; },
    toString() { return this._output.join(''); }
  };
  
  
  class State {
  constructor(components, hooks, templates) {
      this.components = components;
      this.hooks = hooks;
      this.templates = templates;
    }
  }
  
  
  class ComponentInfo {
  constructor(name, csharp_code, templates, hooks) {
      this.name = name;
      this.csharp_code = csharp_code;
      this.templates = templates;
      this.hooks = hooks;
    }
  }
  
  
  class HookInfo {
  constructor(name, hook_type) {
      this.name = name;
      this.hook_type = hook_type;
    }
  }
  
  
  class TemplateInfo {
  constructor(path, template, bindings) {
      this.path = path;
      this.template = template;
      this.bindings = bindings;
    }
  }
  
  // Pre-hook
  
  function init() {
    return { components: [], hooks: [], templates: new Map() };
  }
  
  // Exit hook
  
  function finish() {
    let output = [];
    for (const component of this.state.components) {
      output.push(component.csharp_code);
      output.push("\n");
    }
    return output.toString();
  }
  
  return {
    pre(file) {
      this.state = init(file);
    },
    
    visitor: {
      Program: {
        exit(path, state) {
          return finish.call(this, path.node, state, builder);
        }
      },
      
      FunctionDeclaration(path) {
        const node = path.node;
        const __iflet_0 = process_component(node);
        if (__iflet_0 !== null && __iflet_0 !== undefined) {
          const component = __iflet_0;
          const csharp_code = generate_csharp_file(component);
          const templates = generate_template_map_json(component);
          const info = { name: component.name, csharp_code: csharp_code, templates: templates, hooks: [] };
          this.state.components.push(info);
        }
        /* Babel auto-traverses */;
      },
    }
  };
};
