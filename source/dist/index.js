// Module imports
const fs = require('fs');
const json = {
  stringify: (obj) => JSON.stringify(obj, null, 2),
  parse: (str) => JSON.parse(str)
};

// Generated by ReluxScript compiler
// Do not edit manually

module.exports = function({ types: t }) {
  
  let state = {};
  
  return {
    visitor: {
      Identifier(path) {
        const node = path.node;
        const result = fs.writeFileSync("test.txt", "hello");
      },
      StringLiteral(path) {
        const node = path.node;
        const data = stringify(node.value);
      }
    }
  };
};
