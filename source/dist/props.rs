// Generated by ReluxScript compiler
// Do not edit manually
// NOTE: SWC plugins require nightly Rust

use swc_common::{Span, DUMMY_SP, SyntaxContext};
use swc_ecma_ast::*;
use swc_ecma_visit::{Visit, VisitMut, VisitMutWith, VisitWith};

#[derive(Clone, Debug)]
struct PropInfo {
    name: String,
    prop_type: String,
}

pub fn extract_props(params: &Vec<Pat>) -> Vec<PropInfo> {
    let mut props = vec![];
    for param in params {
        match param {
            Pat::Object(obj_pat) => {
                {
                    for prop in &obj_pat.props {
                        match prop {
                            ObjectPatternProperty::KeyValue(kv) => {
                                match &kv.key {
                                    Expr::Ident(id) => {
                                        let prop_type = if kv.value {
                                            "dynamic".into()
                                        } else {
                                            "dynamic".into()
                                        };
                                        props.push(PropInfo { name: id.sym.to_string(), prop_type: prop_type })
                                    }
                                    _ => {}
                                }
                            }
                            _ => {}
                        }
                    }
                }
            }
            Pat::Ident(id) => {
                {
                    props.push(PropInfo { name: id.sym.to_string(), prop_type: "dynamic".into() })
                }
            }
            _ => {
                {
                }
            }
        }
    }
    props
}

