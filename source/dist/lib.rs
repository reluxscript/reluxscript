// Generated by ReluxScript compiler
// Do not edit manually
// NOTE: SWC plugins require nightly Rust

use swc_common::{Span, DUMMY_SP, SyntaxContext};
use swc_ecma_ast::*;
use swc_ecma_visit::{Visit, VisitMut, VisitMutWith, VisitWith};

pub struct TestUnwrapChain {
    output: String,
    indent_level: usize,
}

impl Visit for TestUnwrapChain {}

impl TestUnwrapChain {
    pub fn new() -> Self {
        Self {
            output: String::new(),
            indent_level: 0,
        }
    }
    
    fn append(&mut self, s: &str) {
        self.output.push_str(s);
    }
    
    fn append_line(&mut self, s: &str) {
        for _ in 0..self.indent_level {
            self.output.push_str("    ");
        }
        self.output.push_str(s);
        self.output.push('\n');
    }
    
    fn indent(&mut self) {
        self.indent_level += 1;
    }
    
    fn dedent(&mut self) {
        if self.indent_level > 0 {
            self.indent_level -= 1;
        }
    }
    
    fn newline(&mut self) {
        self.output.push('\n');
    }
    
    pub fn to_string(&self) -> String {
        self.output.clone()
    }
    
    fn visit_mut_call_expr(&mut self, node: &CallExpr) {
        let callee_name = match node.callee.as_expr().unwrap().as_ref() { Expr::Ident(i) => i.sym.to_string(), _ => "".into() };
    }
    
    fn visit_mut_expr_stmt(&mut self, node: &ExprStmt) {
        match node.expr.as_ref() {
            Expr::Call(__inner) => {
                let call = __inner.clone();
                let name = call.callee.name.clone();
            }
            _ => {}
        }
    }
    
}
