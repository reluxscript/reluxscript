// Generated by ReluxScript compiler
// Do not edit manually

module.exports = function({ types: t }) {
  
  class State {
  constructor(rect_count, total_area) {
      this.rect_count = rect_count;
      this.total_area = total_area;
    }
  }
  
  function init() {
    return { rect_count: 0, total_area: 0 };
  }
  
  let state = {};
  
  return {
    visitor: {
      Ident(path) {
        const node = path.node;
        const r = { top_left: { x: 0, y: 0 }, bottom_right: { x: 10, y: 10 } };
        this.state.rect_count = (this.state.rect_count + 1);
        this.state.total_area = (this.state.total_area + area(r));
        if (is_at_origin(r)) {
          this.state.rect_count = (this.state.rect_count + 1);
        }
      }
    }
  };
};
