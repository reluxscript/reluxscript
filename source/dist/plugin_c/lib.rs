// Generated by ReluxScript compiler
// Do not edit manually
// NOTE: SWC plugins require nightly Rust

use swc_common::{Span, DUMMY_SP, SyntaxContext};
use swc_ecma_ast::*;
use swc_ecma_visit::{Visit, VisitMut, VisitMutWith, VisitWith};

mod module_b;
use module_b::{Rectangle, area, is_at_origin, Point};

#[derive(Clone, Debug)]
struct State {
    rect_count: i32,
    total_area: i32,
}

pub struct GeometryChecker {
    pub state: State,
}

impl VisitMut for GeometryChecker {
    fn visit_mut_ident(&mut self, node: &mut Ident) {
        let r = Rectangle { top_left: Point { x: 0, y: 0 }, bottom_right: Point { x: 10, y: 10 } };
        self.state.rect_count = (self.state.rect_count + 1);
        self.state.total_area = (self.state.total_area + area(&r));
        if is_at_origin(&r) {
            self.state.rect_count = (self.state.rect_count + 1)
        }
    }
    
}

impl GeometryChecker {
    pub fn new() -> Self {
        Self {
            state: State {
                rect_count: 0,
                total_area: 0,
            },
        }
    }
    
    fn init() -> State {
        State { rect_count: 0, total_area: 0 }
    }
    
}

